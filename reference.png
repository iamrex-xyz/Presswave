<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>AgentPR — Your Autonomous PR Agent</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Instrument+Serif:ital@0;1&family=DM+Mono:wght@300;400;500&family=Syne:wght@400;600;700;800&display=swap" rel="stylesheet">
<style>
  *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

  :root {
    --ink: #0a0a0a;
    --paper: #f5f2ec;
    --warm: #f0ebe0;
    --accent: #d4521a;
    --accent-light: #f4e4d8;
    --mid: #6b6560;
    --rule: #d8d2c8;
    --green: #1a5c3a;
    --mono: 'DM Mono', monospace;
    --serif: 'Instrument Serif', serif;
    --sans: 'Syne', sans-serif;
  }

  html { scroll-behavior: smooth; }

  body {
    background: var(--paper);
    color: var(--ink);
    font-family: var(--sans);
    min-height: 100vh;
    overflow-x: hidden;
  }

  /* ── NOISE OVERLAY ── */
  body::before {
    content: '';
    position: fixed;
    inset: 0;
    background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noise'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noise)' opacity='0.04'/%3E%3C/svg%3E");
    pointer-events: none;
    z-index: 1000;
    opacity: 0.6;
  }

  /* ── NAV ── */
  nav {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 24px 48px;
    border-bottom: 1px solid var(--rule);
    position: sticky;
    top: 0;
    background: var(--paper);
    z-index: 100;
  }

  .logo {
    font-family: var(--sans);
    font-weight: 800;
    font-size: 18px;
    letter-spacing: -0.02em;
    color: var(--ink);
    text-decoration: none;
  }

  .logo span {
    color: var(--accent);
  }

  .nav-tag {
    font-family: var(--mono);
    font-size: 11px;
    color: var(--mid);
    letter-spacing: 0.08em;
    text-transform: uppercase;
  }

  /* ── HERO ── */
  .hero {
    padding: 80px 48px 60px;
    max-width: 1100px;
    margin: 0 auto;
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 80px;
    align-items: start;
  }

  .hero-left {}

  .hero-eyebrow {
    font-family: var(--mono);
    font-size: 11px;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: var(--accent);
    margin-bottom: 24px;
    display: flex;
    align-items: center;
    gap: 12px;
  }

  .hero-eyebrow::before {
    content: '';
    display: block;
    width: 24px;
    height: 1px;
    background: var(--accent);
  }

  h1 {
    font-family: var(--serif);
    font-size: clamp(48px, 5vw, 72px);
    line-height: 1.05;
    letter-spacing: -0.02em;
    color: var(--ink);
    margin-bottom: 28px;
  }

  h1 em {
    font-style: italic;
    color: var(--accent);
  }

  .hero-sub {
    font-size: 17px;
    line-height: 1.65;
    color: var(--mid);
    max-width: 420px;
    margin-bottom: 40px;
    font-weight: 400;
  }

  .hero-stats {
    display: flex;
    gap: 32px;
    padding-top: 32px;
    border-top: 1px solid var(--rule);
  }

  .stat {}
  .stat-num {
    font-family: var(--serif);
    font-size: 32px;
    color: var(--ink);
    line-height: 1;
    margin-bottom: 4px;
  }
  .stat-label {
    font-family: var(--mono);
    font-size: 10px;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: var(--mid);
  }

  /* ── FORM CARD ── */
  .hero-right {
    position: sticky;
    top: 100px;
  }

  .form-card {
    background: var(--ink);
    color: var(--paper);
    border-radius: 4px;
    padding: 40px;
    position: relative;
    overflow: hidden;
  }

  .form-card::before {
    content: '';
    position: absolute;
    top: -60px;
    right: -60px;
    width: 200px;
    height: 200px;
    background: radial-gradient(circle, rgba(212,82,26,0.15) 0%, transparent 70%);
    pointer-events: none;
  }

  .form-label {
    font-family: var(--mono);
    font-size: 10px;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: rgba(245,242,236,0.4);
    margin-bottom: 8px;
    display: block;
  }

  .form-card h2 {
    font-family: var(--serif);
    font-size: 28px;
    line-height: 1.2;
    margin-bottom: 6px;
    color: var(--paper);
  }

  .form-card p {
    font-size: 13px;
    color: rgba(245,242,236,0.5);
    margin-bottom: 28px;
    line-height: 1.5;
  }

  .field {
    margin-bottom: 16px;
  }

  .field label {
    font-family: var(--mono);
    font-size: 10px;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: rgba(245,242,236,0.45);
    display: block;
    margin-bottom: 6px;
  }

  .field textarea,
  .field input,
  .field select {
    width: 100%;
    background: rgba(255,255,255,0.06);
    border: 1px solid rgba(255,255,255,0.12);
    border-radius: 3px;
    padding: 12px 14px;
    color: var(--paper);
    font-family: var(--sans);
    font-size: 14px;
    outline: none;
    transition: border-color 0.2s, background 0.2s;
    resize: none;
    -webkit-appearance: none;
  }

  .field select {
    cursor: pointer;
  }

  .field select option {
    background: #1a1a1a;
    color: var(--paper);
  }

  .field textarea:focus,
  .field input:focus,
  .field select:focus {
    border-color: var(--accent);
    background: rgba(212,82,26,0.06);
  }

  .field textarea::placeholder,
  .field input::placeholder {
    color: rgba(245,242,236,0.25);
  }

  /* ── CALCULATE BUTTON ── */
  .calc-btn {
    width: 100%;
    background: var(--accent);
    color: white;
    border: none;
    border-radius: 3px;
    padding: 15px 24px;
    font-family: var(--sans);
    font-size: 14px;
    font-weight: 600;
    letter-spacing: 0.02em;
    cursor: pointer;
    transition: all 0.2s;
    margin-top: 8px;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 10px;
    position: relative;
    overflow: hidden;
  }

  .calc-btn:hover {
    background: #b8441a;
    transform: translateY(-1px);
  }

  .calc-btn:active {
    transform: translateY(0);
  }

  .calc-btn .btn-icon {
    font-size: 16px;
    transition: transform 0.3s;
  }

  .calc-btn:hover .btn-icon {
    transform: translateX(4px);
  }

  /* ── RESULT BOX ── */
  .result-box {
    display: none;
    margin-top: 20px;
    background: rgba(255,255,255,0.04);
    border: 1px solid rgba(255,255,255,0.1);
    border-radius: 3px;
    padding: 20px;
    animation: fadeSlideUp 0.4s ease forwards;
  }

  .result-box.visible {
    display: block;
  }

  @keyframes fadeSlideUp {
    from { opacity: 0; transform: translateY(12px); }
    to { opacity: 1; transform: translateY(0); }
  }

  .result-count {
    font-family: var(--serif);
    font-size: 48px;
    color: var(--paper);
    line-height: 1;
    margin-bottom: 4px;
  }

  .result-count span {
    color: var(--accent);
  }

  .result-desc {
    font-family: var(--mono);
    font-size: 11px;
    color: rgba(245,242,236,0.45);
    letter-spacing: 0.06em;
    text-transform: uppercase;
    margin-bottom: 16px;
  }

  .journalist-preview {
    display: flex;
    flex-direction: column;
    gap: 8px;
    margin-bottom: 16px;
  }

  .journalist-item {
    display: flex;
    align-items: center;
    gap: 10px;
    padding: 8px 10px;
    background: rgba(255,255,255,0.04);
    border-radius: 2px;
  }

  .j-dot {
    width: 6px;
    height: 6px;
    border-radius: 50%;
    background: var(--accent);
    flex-shrink: 0;
  }

  .j-name {
    font-family: var(--mono);
    font-size: 12px;
    color: var(--paper);
    flex: 1;
  }

  .j-outlet {
    font-family: var(--mono);
    font-size: 10px;
    color: rgba(245,242,236,0.35);
  }

  .j-more {
    font-family: var(--mono);
    font-size: 11px;
    color: rgba(245,242,236,0.35);
    text-align: center;
    padding: 4px;
  }

  .pay-btn {
    width: 100%;
    background: var(--green);
    color: white;
    border: none;
    border-radius: 3px;
    padding: 14px 24px;
    font-family: var(--sans);
    font-size: 14px;
    font-weight: 600;
    cursor: pointer;
    transition: all 0.2s;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 8px;
  }

  .pay-btn:hover {
    background: #134a2e;
    transform: translateY(-1px);
  }

  .pay-note {
    font-family: var(--mono);
    font-size: 10px;
    color: rgba(245,242,236,0.3);
    text-align: center;
    margin-top: 10px;
    letter-spacing: 0.06em;
  }

  /* ── HOW IT WORKS ── */
  .section {
    padding: 80px 48px;
    max-width: 1100px;
    margin: 0 auto;
  }

  .section-header {
    display: flex;
    align-items: baseline;
    gap: 20px;
    margin-bottom: 56px;
    padding-bottom: 20px;
    border-bottom: 1px solid var(--rule);
  }

  .section-num {
    font-family: var(--mono);
    font-size: 11px;
    color: var(--accent);
    letter-spacing: 0.1em;
  }

  .section-title {
    font-family: var(--serif);
    font-size: 36px;
    color: var(--ink);
  }

  .steps {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 2px;
  }

  .step {
    background: var(--warm);
    padding: 32px 28px;
    position: relative;
    transition: background 0.2s;
  }

  .step:hover {
    background: var(--accent-light);
  }

  .step-num {
    font-family: var(--mono);
    font-size: 11px;
    color: var(--accent);
    letter-spacing: 0.1em;
    margin-bottom: 20px;
  }

  .step-icon {
    font-size: 28px;
    margin-bottom: 16px;
    display: block;
  }

  .step h3 {
    font-family: var(--sans);
    font-size: 15px;
    font-weight: 700;
    color: var(--ink);
    margin-bottom: 10px;
    letter-spacing: -0.01em;
  }

  .step p {
    font-size: 13px;
    line-height: 1.6;
    color: var(--mid);
  }

  /* ── PRICING ── */
  .pricing-section {
    padding: 80px 48px;
    background: var(--ink);
    color: var(--paper);
  }

  .pricing-inner {
    max-width: 1100px;
    margin: 0 auto;
  }

  .pricing-section .section-header {
    border-bottom-color: rgba(255,255,255,0.1);
  }

  .pricing-section .section-title {
    color: var(--paper);
  }

  .pricing-cards {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 2px;
  }

  .pricing-card {
    background: rgba(255,255,255,0.04);
    padding: 36px 32px;
    transition: background 0.2s;
    position: relative;
  }

  .pricing-card.featured {
    background: var(--accent);
  }

  .pricing-card:hover:not(.featured) {
    background: rgba(255,255,255,0.07);
  }

  .pricing-badge {
    font-family: var(--mono);
    font-size: 10px;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: rgba(245,242,236,0.5);
    margin-bottom: 20px;
  }

  .pricing-card.featured .pricing-badge {
    color: rgba(255,255,255,0.7);
  }

  .price {
    font-family: var(--serif);
    font-size: 52px;
    line-height: 1;
    color: var(--paper);
    margin-bottom: 4px;
  }

  .price-sub {
    font-family: var(--mono);
    font-size: 11px;
    color: rgba(245,242,236,0.4);
    margin-bottom: 28px;
    letter-spacing: 0.06em;
  }

  .pricing-card.featured .price-sub {
    color: rgba(255,255,255,0.6);
  }

  .pricing-features {
    list-style: none;
    display: flex;
    flex-direction: column;
    gap: 10px;
    margin-bottom: 32px;
  }

  .pricing-features li {
    font-size: 13px;
    color: rgba(245,242,236,0.7);
    display: flex;
    align-items: center;
    gap: 10px;
  }

  .pricing-card.featured .pricing-features li {
    color: rgba(255,255,255,0.85);
  }

  .pricing-features li::before {
    content: '→';
    color: var(--accent);
    font-size: 12px;
    flex-shrink: 0;
  }

  .pricing-card.featured .pricing-features li::before {
    color: rgba(255,255,255,0.6);
  }

  .pricing-btn {
    width: 100%;
    background: rgba(255,255,255,0.08);
    color: var(--paper);
    border: 1px solid rgba(255,255,255,0.15);
    border-radius: 3px;
    padding: 12px;
    font-family: var(--sans);
    font-size: 13px;
    font-weight: 600;
    cursor: pointer;
    transition: all 0.2s;
    letter-spacing: 0.02em;
  }

  .pricing-card.featured .pricing-btn {
    background: var(--ink);
    border-color: var(--ink);
    color: var(--paper);
  }

  .pricing-btn:hover {
    background: rgba(255,255,255,0.14);
  }

  .pricing-card.featured .pricing-btn:hover {
    background: #1a1a1a;
  }

  /* ── SOCIAL PROOF ── */
  .proof-section {
    padding: 80px 48px;
    max-width: 1100px;
    margin: 0 auto;
  }

  .proof-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 2px;
  }

  .proof-card {
    background: var(--warm);
    padding: 32px 28px;
  }

  .proof-quote {
    font-family: var(--serif);
    font-size: 17px;
    font-style: italic;
    line-height: 1.55;
    color: var(--ink);
    margin-bottom: 20px;
  }

  .proof-attr {
    font-family: var(--mono);
    font-size: 11px;
    color: var(--mid);
    letter-spacing: 0.06em;
  }

  .proof-attr strong {
    display: block;
    color: var(--ink);
    font-style: normal;
    font-family: var(--sans);
    font-size: 13px;
    font-weight: 600;
    margin-bottom: 2px;
  }

  /* ── FOOTER ── */
  footer {
    border-top: 1px solid var(--rule);
    padding: 32px 48px;
    display: flex;
    align-items: center;
    justify-content: space-between;
  }

  .footer-logo {
    font-family: var(--sans);
    font-weight: 800;
    font-size: 16px;
    color: var(--ink);
  }

  .footer-logo span { color: var(--accent); }

  .footer-note {
    font-family: var(--mono);
    font-size: 11px;
    color: var(--mid);
    letter-spacing: 0.06em;
  }

  /* ── TICKER ── */
  .ticker-wrap {
    overflow: hidden;
    border-top: 1px solid var(--rule);
    border-bottom: 1px solid var(--rule);
    background: var(--warm);
    padding: 12px 0;
  }

  .ticker {
    display: flex;
    gap: 48px;
    animation: ticker 20s linear infinite;
    white-space: nowrap;
    width: max-content;
  }

  @keyframes ticker {
    0% { transform: translateX(0); }
    100% { transform: translateX(-50%); }
  }

  .ticker-item {
    font-family: var(--mono);
    font-size: 11px;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: var(--mid);
    display: flex;
    align-items: center;
    gap: 16px;
  }

  .ticker-item::after {
    content: '·';
    color: var(--accent);
  }

  /* ── LOADING DOTS ── */
  .dots {
    display: inline-flex;
    gap: 4px;
    align-items: center;
  }

  .dots span {
    width: 4px;
    height: 4px;
    background: white;
    border-radius: 50%;
    animation: dotBounce 1.2s infinite;
  }

  .dots span:nth-child(2) { animation-delay: 0.2s; }
  .dots span:nth-child(3) { animation-delay: 0.4s; }

  @keyframes dotBounce {
    0%, 80%, 100% { transform: scale(0.8); opacity: 0.4; }
    40% { transform: scale(1.2); opacity: 1; }
  }

  /* ── ANIMATIONS ── */
  .fade-in {
    opacity: 0;
    transform: translateY(20px);
    animation: fadeIn 0.6s ease forwards;
  }

  @keyframes fadeIn {
    to { opacity: 1; transform: translateY(0); }
  }

  .fade-in:nth-child(1) { animation-delay: 0.1s; }
  .fade-in:nth-child(2) { animation-delay: 0.2s; }
  .fade-in:nth-child(3) { animation-delay: 0.3s; }
  .fade-in:nth-child(4) { animation-delay: 0.4s; }

  @media (max-width: 900px) {
    .hero { grid-template-columns: 1fr; gap: 48px; }
    .hero-right { position: static; }
    .steps { grid-template-columns: 1fr 1fr; }
    .pricing-cards { grid-template-columns: 1fr; }
    .proof-grid { grid-template-columns: 1fr; }
    nav { padding: 20px 24px; }
    .hero, .section, .proof-section { padding-left: 24px; padding-right: 24px; }
    .pricing-section { padding-left: 24px; padding-right: 24px; }
    footer { padding: 24px; flex-direction: column; gap: 12px; text-align: center; }
  }
</style>
</head>
<body>

<!-- NAV -->
<nav>
  <a href="#" class="logo">Agent<span>PR</span></a>
  <span class="nav-tag">500,000+ journalists · AI-matched</span>
</nav>

<!-- TICKER -->
<div class="ticker-wrap">
  <div class="ticker">
    <span class="ticker-item">TechCrunch</span>
    <span class="ticker-item">Wired</span>
    <span class="ticker-item">The Verge</span>
    <span class="ticker-item">VentureBeat</span>
    <span class="ticker-item">Forbes</span>
    <span class="ticker-item">MIT Tech Review</span>
    <span class="ticker-item">Hacker News</span>
    <span class="ticker-item">Product Hunt</span>
    <span class="ticker-item">Ars Technica</span>
    <span class="ticker-item">Fast Company</span>
    <span class="ticker-item">TechCrunch</span>
    <span class="ticker-item">Wired</span>
    <span class="ticker-item">The Verge</span>
    <span class="ticker-item">VentureBeat</span>
    <span class="ticker-item">Forbes</span>
    <span class="ticker-item">MIT Tech Review</span>
    <span class="ticker-item">Hacker News</span>
    <span class="ticker-item">Product Hunt</span>
    <span class="ticker-item">Ars Technica</span>
    <span class="ticker-item">Fast Company</span>
  </div>
</div>

<!-- HERO -->
<div class="hero">
  <div class="hero-left">
    <div class="hero-eyebrow fade-in">Autonomous PR for founders</div>
    <h1 class="fade-in">Your <em>autonomous</em> PR agent.</h1>
    <p class="hero-sub fade-in">Tell me about your new product and I'll find the right journalists to tell about it. No agencies. No guesswork. No hours lost in spreadsheets.</p>
    <div class="hero-stats fade-in">
      <div class="stat">
        <div class="stat-num">500k</div>
        <div class="stat-label">Journalists</div>
      </div>
      <div class="stat">
        <div class="stat-num">48h</div>
        <div class="stat-label">Launch window</div>
      </div>
      <div class="stat">
        <div class="stat-num">10 min</div>
        <div class="stat-label">To ready-to-send</div>
      </div>
    </div>
  </div>

  <div class="hero-right">
    <div class="form-card">
      <span class="form-label">Step 1 of 2</span>
      <h2>Describe your product</h2>
      <p>Rex will scan 500,000 journalists and return your exact matches.</p>

      <div class="field">
        <label>What did you build?</label>
        <textarea id="product-desc" rows="4" placeholder="e.g. An AI tool that turns meeting notes into structured action items. Built for solo founders and small teams. Launched on Product Hunt last week."></textarea>
      </div>

      <div class="field">
        <label>Category</label>
        <select id="category">
          <option value="">Select a category...</option>
          <option value="ai-productivity">AI Productivity</option>
          <option value="ai-developer">AI Developer Tools</option>
          <option value="ai-consumer">AI Consumer App</option>
          <option value="saas">SaaS / B2B</option>
          <option value="no-code">No-Code / Low-Code</option>
          <option value="open-source">Open Source</option>
          <option value="other">Other</option>
        </select>
      </div>

      <div class="field">
        <label>Your name / product name</label>
        <input type="text" id="product-name" placeholder="e.g. Alex — MeetingMind">
      </div>

      <button class="calc-btn" id="calc-btn" onclick="calculateMatches()">
        <span>Find my journalists</span>
        <span class="btn-icon">→</span>
      </button>

      <!-- RESULT -->
      <div class="result-box" id="result-box">
        <div class="result-count" id="result-count"><span>0</span></div>
        <div class="result-desc">journalists matched to your product</div>

        <div class="journalist-preview" id="journalist-preview"></div>

        <button class="pay-btn" onclick="handlePay()">
          <span>Get all contacts — €29</span>
          <span>↓</span>
        </button>
        <div class="pay-note">Instant CSV download · Personalised pitches included</div>
      </div>
    </div>
  </div>
</div>

<!-- HOW IT WORKS -->
<div class="section">
  <div class="section-header">
    <span class="section-num">01</span>
    <h2 class="section-title">How Rex works</h2>
  </div>
  <div class="steps">
    <div class="step">
      <div class="step-num">01</div>
      <span class="step-icon">✍️</span>
      <h3>Describe your product</h3>
      <p>Tell Rex what you built, who it's for, and what makes it different. Plain language, two sentences is enough.</p>
    </div>
    <div class="step">
      <div class="step-num">02</div>
      <span class="step-icon">🔍</span>
      <h3>Rex scans 500k journalists</h3>
      <p>Rex matches your product against beat tags, recent coverage, and outlet tier. Returns only journalists likely to actually cover you.</p>
    </div>
    <div class="step">
      <div class="step-num">03</div>
      <span class="step-icon">✉️</span>
      <h3>Get your personalised pack</h3>
      <p>Download your contact list with personalised pitch copy per journalist. Ready to send from your own inbox in minutes.</p>
    </div>
    <div class="step">
      <div class="step-num">04</div>
      <span class="step-icon">📈</span>
      <h3>Get coverage</h3>
      <p>Send from your own account. Your domain, your reputation. Every campaign makes Rex smarter for next time.</p>
    </div>
  </div>
</div>

<!-- PRICING -->
<div class="pricing-section">
  <div class="pricing-inner">
    <div class="section-header">
      <span class="section-num" style="color: rgba(245,242,236,0.4)">02</span>
      <h2 class="section-title">Simple pricing</h2>
    </div>
    <div class="pricing-cards">
      <div class="pricing-card">
        <div class="pricing-badge">Free</div>
        <div class="price">€0</div>
        <div class="price-sub">Forever free</div>
        <ul class="pricing-features">
          <li>1 journalist search</li>
          <li>3 matched contacts preview</li>
          <li>See who Rex finds</li>
          <li>No credit card required</li>
        </ul>
        <button class="pricing-btn">Start free</button>
      </div>
      <div class="pricing-card featured">
        <div class="pricing-badge">Most popular</div>
        <div class="price">€29</div>
        <div class="price-sub">Per campaign · one-off</div>
        <ul class="pricing-features">
          <li>Full journalist match list</li>
          <li>Personalised pitch per contact</li>
          <li>Send instructions included</li>
          <li>CSV + Google Doc export</li>
          <li>Follow-up templates</li>
        </ul>
        <button class="pricing-btn">Get this campaign</button>
      </div>
      <div class="pricing-card">
        <div class="pricing-badge">Unlimited</div>
        <div class="price">€79</div>
        <div class="price-sub">Per month</div>
        <ul class="pricing-features">
          <li>Unlimited campaigns</li>
          <li>Priority journalist matching</li>
          <li>Placement tracking</li>
          <li>Signal Intelligence reports</li>
          <li>Podcast outreach (soon)</li>
        </ul>
        <button class="pricing-btn">Go unlimited</button>
      </div>
    </div>
  </div>
</div>

<!-- SOCIAL PROOF -->
<div class="proof-section">
  <div class="section-header">
    <span class="section-num">03</span>
    <h2 class="section-title">Founders who shipped</h2>
  </div>
  <div class="proof-grid">
    <div class="proof-card">
      <p class="proof-quote">"I described my tool in two sentences. Rex came back with 24 journalists who had literally covered the exact problem I'm solving. TechCrunch was in the list."</p>
      <div class="proof-attr">
        <strong>Mia K.</strong>
        Founder, AI writing tool · Product Hunt #3 of the day
      </div>
    </div>
    <div class="proof-card">
      <p class="proof-quote">"Spent two weeks trying to find the right journalists manually. AgentPR did it in 10 minutes. The personalised pitches were better than what I'd written myself."</p>
      <div class="proof-attr">
        <strong>James T.</strong>
        Solo founder, developer tools · Featured in The Verge
      </div>
    </div>
    <div class="proof-card">
      <p class="proof-quote">"The moment my launch window opened I had 19 ready-to-send pitches. Sent them from my own Gmail. Got 4 responses in the first 24 hours."</p>
      <div class="proof-attr">
        <strong>Priya M.</strong>
        Indie hacker, AI productivity · Covered by VentureBeat
      </div>
    </div>
  </div>
</div>

<!-- FOOTER -->
<footer>
  <div class="footer-logo">Agent<span>PR</span></div>
  <div class="footer-note">A Napkin Labs experiment · Built by Rex</div>
</footer>

<script>
  const JOURNALIST_SAMPLES = {
    'ai-productivity': [
      { name: 'Sarah Perez', outlet: 'TechCrunch' },
      { name: 'Janko Roettgers', outlet: 'The Verge' },
      { name: 'Kyle Wiggers', outlet: 'VentureBeat' },
    ],
    'ai-developer': [
      { name: 'Devin Coldewey', outlet: 'TechCrunch' },
      { name: 'Tom Warren', outlet: 'The Verge' },
      { name: 'Matt Marshall', outlet: 'VentureBeat' },
    ],
    'saas': [
      { name: 'Ron Miller', outlet: 'TechCrunch' },
      { name: 'Ingrid Lunden', outlet: 'TechCrunch' },
      { name: 'Alex Konrad', outlet: 'Forbes' },
    ],
    'default': [
      { name: 'Natasha Mascarenhas', outlet: 'TechCrunch' },
      { name: 'Aisha Malik', outlet: 'TechCrunch' },
      { name: 'Maxwell Zeff', outlet: 'Wired' },
    ]
  };

  function getCount(text, category) {
    // Simulate intelligent matching based on description length + category
    const base = 18;
    const textBonus = Math.min(Math.floor(text.length / 40), 8);
    const categoryBonus = category && category !== '' ? 4 : 0;
    const noise = Math.floor(Math.random() * 5);
    return base + textBonus + categoryBonus + noise;
  }

  function animateCount(target, el) {
    let current = 0;
    const step = Math.ceil(target / 30);
    const timer = setInterval(() => {
      current = Math.min(current + step, target);
      el.querySelector('span').textContent = current;
      if (current >= target) clearInterval(timer);
    }, 30);
  }

  function calculateMatches() {
    const desc = document.getElementById('product-desc').value.trim();
    const category = document.getElementById('category').value;
    const name = document.getElementById('product-name').value.trim();

    if (!desc) {
      document.getElementById('product-desc').focus();
      document.getElementById('product-desc').style.borderColor = 'var(--accent)';
      return;
    }

    const btn = document.getElementById('calc-btn');
    btn.innerHTML = '<span class="dots"><span></span><span></span><span></span></span>';
    btn.disabled = true;

    setTimeout(() => {
      const count = getCount(desc, category);
      const samples = JOURNALIST_SAMPLES[category] || JOURNALIST_SAMPLES['default'];

      const previewEl = document.getElementById('journalist-preview');
      previewEl.innerHTML = samples.map(j => `
        <div class="journalist-item">
          <div class="j-dot"></div>
          <span class="j-name">${j.name}</span>
          <span class="j-outlet">${j.outlet}</span>
        </div>
      `).join('') + `<div class="j-more">+ ${count - 3} more journalists unlocked on purchase</div>`;

      const resultBox = document.getElementById('result-box');
      resultBox.classList.add('visible');

      animateCount(count, document.getElementById('result-count'));

      btn.innerHTML = '<span>Recalculate</span><span class="btn-icon">→</span>';
      btn.disabled = false;

      resultBox.scrollIntoView({ behavior: 'smooth', block: 'nearest' });
    }, 1800);
  }

  function handlePay() {
    alert('Payment flow — connect Stripe here.\n\nFounder receives CSV download with:\n• Full journalist list\n• Personalised pitch per contact\n• Send instructions & follow-up templates');
  }

  // Allow Enter in textarea to not submit, but Cmd+Enter to trigger
  document.getElementById('product-desc').addEventListener('keydown', function(e) {
    if ((e.metaKey || e.ctrlKey) && e.key === 'Enter') {
      calculateMatches();
    }
  });
</script>
</body>
</html>
